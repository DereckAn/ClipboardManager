<?xml version="1.0" encoding="utf-8"?>
<UserControl
      x:Class="Clipboard.Views.Controls.FavoritesPanel"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Clipboard.Views.Controls"
      xmlns:viewmodels="using:Clipboard.ViewModels"
      xmlns:controls="using:Clipboard.Views.Controls"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <controls:ClipboardListPanel 
          ItemsSource="{x:Bind ViewModel.FilteredFavorites}"
          SelectedItem="{x:Bind ViewModel.SelectedItem, Mode=TwoWay}"
          EmptyStateIcon="⭐"
          EmptyStateText="Selecciona un favorito para ver los detalles"
          CounterText="⭐ favoritos"
          CopyToClipboardCommand="{x:Bind ViewModel.CopyToClipboardCommand}"
          ToggleFavoriteCommand="{x:Bind ViewModel.ToggleFavoriteCommand}"
          DeleteItemCommand="{x:Bind ViewModel.DeleteItemCommand}"/>
</UserControl>